{
  "private": true,
  "description": "No direct npm deps. io-multiformats uses org-nist-sha2 on :cljs (ADR-2608260200 tranche 2).",
  "dependencies": {},
  "scripts": {
    "smoke": "nbb --classpath \"$(clojure -Spath | tr ':' '\\n' | grep -E '(^src$|gitlibs)' | tr '\\n' ':')\" test/nbb_smoke.cljs"
  }
}
